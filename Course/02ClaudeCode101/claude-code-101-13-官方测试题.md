# Claude Code 101 - 13 Official Quiz 官方测试题

> Course: Claude Code 101 · Lesson 13
> 课程: Claude Code 101 · 第 13 课
> 来源: Anthropic Academy (anthropic.skilljar.com),用户登录后提供英文原文,2026-07-12 翻译存档
> 说明: 原文未标注正确答案(需在平台上作答后由系统判定),本文件仅做题目与选项的中英对照翻译。

## 1. Claude Code works as an AI agent. What is an AI agent?

Claude Code 是以 AI 智能体(AI agent)的方式工作的。什么是 AI 智能体?

- A. A chatbot that responds to questions in real time
  一个能实时回答问题的聊天机器人
- B. A code editor with built-in autocomplete features
  一款内置自动补全功能的代码编辑器
- C. A cloud service that hosts your development projects
  一项托管你开发项目的云服务
- D. AI that takes action to complete goals
  一种会采取行动以完成目标的 AI

## 2. What happens when Claude Code reaches its context window limit?

当 Claude Code 达到上下文窗口上限时,会发生什么?

- A. It switches to a smaller, faster model to save memory
  它会切换到一个更小、更快的模型以节省内存
- B. It removes your oldest files to make room for new ones
  它会删除你最早的文件,为新文件腾出空间
- C. It automatically compacts your conversation to free up space
  它会自动压缩你的对话,以腾出空间
- D. It stops working and asks you to restart the session
  它会停止工作,并要求你重启会话

## 3. What is the recommended workflow for using Claude Code effectively?

高效使用 Claude Code 的推荐工作流是什么?

- A. Code → Test → Deploy → Monitor
  编码 → 测试 → 部署 → 监控
- B. Explore → Plan → Code → Commit
  探索 → 计划 → 编码 → 提交
- C. Write → Review → Merge → Ship
  编写 → 审查 → 合并 → 发布
- D. Prompt → Accept → Push → Repeat
  提示 → 接受 → 推送 → 重复

## 4. How does Claude Code use the CLAUDE.md file?

Claude Code 是如何使用 CLAUDE.md 文件的?

- A. It reads it only after you run the /init command
  只有在你运行 /init 命令之后才会读取它
- B. It reads it once when you first create the project
  只在你第一次创建项目时读取一次
- C. It reads it automatically at the start of every session
  它会在每次会话开始时自动读取
- D. It only reads it when you explicitly ask it to
  只有当你明确要求时才会读取它
